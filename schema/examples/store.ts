import { Store } from '../../src/models/index';

const store: Store = {
  id: 'store-1',
  name: 'Example Mart',
  searchUrl: 'https://example.com/search?q={query}',
  version: 1,
};

export default store;
