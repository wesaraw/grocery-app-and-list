import { MealOverride } from '../../src/models/index';

const mealOverride: MealOverride = {
  id: 'mo-1',
  userId: 'user-1',
  categoryId: 'cat-1',
  weekNumber: 1,
  mealIds: ['meal-1'],
  version: 1,
};

export default mealOverride;
